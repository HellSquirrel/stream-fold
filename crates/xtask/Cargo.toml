[package]
name = "xtask"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "Repository tasks: generate the pages' contract fragments from the components' manifests."
publish = false

[dependencies]
logfold-core = { path = "../logfold-core" }
studio = { path = "../studio" }
