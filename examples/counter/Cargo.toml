[package]
name = "counter"
edition.workspace = true
version.workspace = true
license.workspace = true
description = "A second component, to show what adding one costs: three inputs, one number, CSS renders it."

[dependencies]
logfold-core = { path = "../../crates/logfold-core" }
